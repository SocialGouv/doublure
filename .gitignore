# Sorties de capture egress — peuvent contenir du trafic de session réel
captures/

# Clones de référence et upstream GPL (jamais committés ici)
.repos/
services/anonshield/upstream/

# Corpus réel (Phase 5) — seules les annotations synthétiques sont versionnées
corpus/real/

# Python
__pycache__/
*.pyc
.venv/

# Venv jetable des essais de modèles (transformers >= 5, hors du lock projet)
.venv-pf/

# devbox — environnement local (nix), jamais committé
.devbox/
dist/

# Site de documentation construit (publié par la CI sur gh-pages)
site/

# Venv du service de données personnelles (transformers >= 5, hors du lock projet)
services/pii/.venv/
